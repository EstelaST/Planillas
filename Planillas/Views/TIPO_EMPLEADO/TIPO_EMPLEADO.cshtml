@model IEnumerable<Planillas.TIPO_EMPLEADO>

@{
    ViewBag.Title = "TIPO_EMPLEADO";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Tipo de Empleado</h2>

<p>
    <button type="button" class="btn btn-primary"> @Html.ActionLink("Nuevo", "Create")</button>
</p>
<table class="table">
    <tr>
        <th>
            Nombre
        </th>
        <th>
            Sueldo
        </th>
        <th></th>
    </tr>

    @if (Model != null)
    {
        foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.NOMBRE_TIPO_EMPLEADO)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SUELDO)
                </td>
                <td>
                    <button type="button" class="btn btn-success">@Html.ActionLink("Modificar", "Edit", new { id = item.CODIGO_TIPO_EMPLEADO })</button>
                    <button type="button" class="btn btn-secondary">@Html.ActionLink("Detalles", "Details", new { id = item.CODIGO_TIPO_EMPLEADO })</button>
                    <button type="button" class="btn btn-danger">@Html.ActionLink("Eliminar", "Delete", new { id = item.CODIGO_TIPO_EMPLEADO })</button>
                </td>
            </tr>
        }
    }

</table>
